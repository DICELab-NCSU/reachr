# Write simple features spatial data to an Emlid Flow-compatible CSV file

Write simple features spatial data to an Emlid Flow-compatible CSV file

## Usage

``` r
write_emlid_csv(x, file)
```

## Arguments

- x:

  An sf object.

- file:

  string. Path to file containing spatial data.

## Value

Invisible
